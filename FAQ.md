# 常見問題（FAQ）

這裡收一些常被問到的問題，老實回答。跟站上的[安心使用聲明](https://web-production-c1d3c.up.railway.app/about)是同一套說法，不會有兩套話。

---

## 一般使用

### 這不就跟其他讀經 App 一樣？

市面上讀經 App 不少，這個做的是一個很窄的情境：**小組一起讀同一段經文、非同步看見彼此的領受，而且完全沒有打卡壓力**。不是要取代誰，合用再用就好。

### 怎麼開始？

登入後如果你還沒有小組，可以：

- **用加入碼加入**：跟你的小組輔導拿一組六碼加入碼，貼進去就加入。
- **自己開一組**：填個小組名字就建好，你會自動成為這一組的輔導，並拿到一組加入碼分享給組員。

### 輔導怎麼讓組員加入？

到後台複製你這一組的加入碼，貼到小組群裡。組員各自登入後貼上加入碼就進來了，不用輔導一個一個手動加。

### 這個是免費的嗎？會不會之後收費？

免費。這是個人專案，沒有商業模式、沒有金主，也沒打算靠它賺錢。真的哪天養不起伺服器，我會提前講，並保證匯出功能一直都在，讓大家把資料帶走。

---

## 隱私與資安

### 你（開發者）看得到我寫的領受嗎？

老實說：**看得到**。我有資料庫的管理權限，技術上是能存取的，我不會假裝「連我都看不到」。這不是一個端對端加密的產品。

我能給的承諾是：不賣資料、不拿去做任何其他用途。而且**程式碼是開源的，你可以直接看它到底怎麼運作**，不是我嘴巴說了算。你也可以隨時一鍵匯出、或自己刪掉自己的領受。

### 別的小組看得到我們的領受嗎？

看不到。每個小組靠加入碼各自獨立，**所有查詢都以「你這一組」為界**——你只看得到自己這一組的經文與領受，陌生人登入也看不到你們的。這件事有自動化測試每次都跑一遍守著。

### 為什麼用 LINE 登入？安全嗎？

因為小組本來就都在用 LINE，門檻最低。它只跟 LINE 要**最小的資料**（顯示名稱、頭像、一組帳號代碼），拿不到你的 email、電話、通訊錄，也不會用你的帳號發任何訊息。不想露本名的話可以設暱稱。

### 哪天服務收掉，這些領受不就沒了？

這點一開始就很在意，所以「**一鍵匯出 ＋ 離線閱讀器**」現在就能用——可以匯出成一頁不需要網路就能打開的檔案，存到自己電腦，不需要懂程式也能把整組的領受留下來、交給下一個接手的人。

### 你們做了哪些資安管控？

- 程式碼開源可檢視，所有金鑰放環境變數、不寫進程式碼
- 多小組隔離，每一次查詢都以「你這一組」為界（有自動化測試守著）
- 正式站一定要設隨機密鑰才准啟動，避免有人偽造身分冒充管理員
- Cookie 加固（HttpOnly、SameSite、正式站只走 HTTPS）
- 所有寫入動作都有 CSRF 保護
- 匯出的 CSV 有防公式注入
- 資料庫預設一律拒絕（RLS），只有後端能碰，前端不直接連資料庫
- 自動化測試強制跑在記憶體模式，不會碰到正式站的資料

---

## 給工程師的

### AI 寫的 / vibe coder 做的，能信嗎？

不否認是靠 AI 寫的，但安全這塊有刻意當一回事：多租戶每查詢隔離、CSRF、cookie 加固、CSV 防注入、RLS 預設拒絕、測試不碰正式庫，全部有自動化測試跟 CI 在跑。程式碼開源，**歡迎直接開 issue，有洞很想知道**。

### 技術架構？

- Flask + Jinja2 + 純 HTML/CSS/JS（沒用前端框架）
- Supabase（PostgreSQL），service-role 只在後端用，RLS 預設拒絕
- 真 PWA（manifest + service worker，可加到主畫面、有離線頁）
- LINE Login OAuth
- pytest 測試套件 + GitHub Actions CI

更多細節看 [README](README.md)。

### 我發現一個 bug / 資安問題，怎麼回報？

到 [GitHub repo](https://github.com/windsjp00171-star/group-devotion) 開一個 issue。資安相關的問題特別歡迎，會認真看。

---

## 給輔導 / 信仰相關

### AI 生成的引導問題，會不會亂解經？

引導問題只是「幫你停下來想一想」的開放式提問，**不是解經、不下結論、也不代替講道**。輔導排經文時看到不合適的隨時可以改成自己的。經文本身一律是和合本原文，系統**不讓任何人（包含輔導）改動聖經字句**。

### 輔導可以做什麼？

- 排每天的經文（選書卷＋章節自動帶出和合本全文，或批次匯入一次排好幾天）
- 移除不當內容（安靜處理，不公開標記、不通知圍觀，是牧養不是公審）
- 禁言自己組內的成員（不是封鎖，被禁言的人還是能讀、能看，只是暫時不能留新領受）
- 把組員升成共同輔導、改自己這組的組名

### 用哪個版本的聖經？

和合本（1919，公有領域）。聖經文本本身沒有版權問題，可以自由使用。

---

有其他問題歡迎到 [GitHub](https://github.com/windsjp00171-star/group-devotion) 開 issue 問。
